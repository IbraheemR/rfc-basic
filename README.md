# RFC basic Sumo Robo firmware

PlatformIO + astrojs

# Flash firmware

Use PIO as normal

# Flash webcontrols to LittleFS

run
```
cd web
npm i
npm run build

cd ..
```

Then use `Upload Filesystem` option from PIO menu